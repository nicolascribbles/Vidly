@model Vidly2.ViewModels.MovieFormViewModel

@{
    ViewBag.Title = "Movies";
}
    <div class="container pt-5">
        @if (Model.Movies == null || Model.Movies.Id == 0)
        {

            <h2>New Movie</h2>
        }
        else
        {
            <h2>Edit Movie </h2>
        }
        @using (Html.BeginForm("Save", "Movies"))
        {
            @Html.HiddenFor(m => m.Movies.Id)


            <div class="form-group">
                @Html.LabelFor(m => m.Movies.Name)
                @Html.TextBoxFor(m => m.Movies.Name, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Movies.ReleaseDate)
                @Html.TextBoxFor(m => m.Movies.ReleaseDate, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Movies.GenreId)
                @Html.DropDownListFor(m => m.Movies.GenreId, new SelectList(Model.Genres, "Id", "Name"), "Select the genre", new { @class = "form-control" })
            </div>
            <div class="form-group">

                @Html.LabelFor(m => m.Movies.NumberAvailable)
                @Html.TextBoxFor(m => m.Movies.NumberAvailable, new { @class = "form-control" })
            </div>
            <button type="submit" class="btn btn-primary mt-5">Save</button>
        }
    </div>